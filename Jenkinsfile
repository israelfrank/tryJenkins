pipeline {
   agent any
   stages {
    stage('Build') {
      steps {
       script {
        if(env.GIT_BRANCH == 'master' ) {
          echo env.GIT_BRANCH
         }
            
        }
       }
      }
     }
    }
  
